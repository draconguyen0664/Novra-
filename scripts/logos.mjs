import {chromium}from'playwright';import fs from'node:fs/promises';const b=await chromium.launch({headless:true,executablePath:'C:/Users/Admin/AppData/Local/ms-playwright/chromium-1234/chrome-win64/chrome.exe'});const p=await b.newPage({viewport:{width:1440,height:900}});await p.goto('https://wama.com.br/',{waitUntil:'networkidle'});const logos=await p.locator('#clientes [data-framer-name="C1 Front"]').evaluateAll(es=>es.map(e=>({html:e.innerHTML,svg:e.querySelector('svg')?.outerHTML,img:e.querySelector('img')?.src})));console.log(logos.map((l,i)=>({i,svg:!!l.svg,img:l.img?.slice(0,65),html:l.html.slice(0,140)})));await fs.writeFile('docs/reference/logo-elements.json',JSON.stringify(logos));const brand=await p.locator('nav a').first().innerHTML();await fs.writeFile('docs/reference/brand.html',brand);await b.close();
