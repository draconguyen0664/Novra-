export default function Head() {
  // Shared measured styles for the below-the-fold client islands.
  // eslint-disable-next-line @next/next/no-css-tags
  return <link rel="stylesheet" href="/lower-sections.css" />;
}
