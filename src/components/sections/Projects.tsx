import Image from 'next/image';import content from '@/data/content.json';import { SectionHeading } from '@/components/ui/SectionHeading';
export function Projects(){return <section id="cases" className="section projects"><div className="shell"><SectionHeading label="PROJETOS">Superando padrões.<br/>Não apenas expectativas.</SectionHeading><div className="project-grid">{content.projects.map((p,i)=><article className={'project-card '+(i%3===2?'project-wide':'')} key={p.title}><a href={p.href} aria-label={'Conheça o projeto '+p.title}><div className="project-media">{p.image&&<Image src={p.image.src} alt={p.image.alt} fill sizes={i%3===2?'90vw':'(max-width:809px) 90vw, 45vw'}/>} {p.video&&<video data-preview muted loop playsInline preload="none" poster={p.image?.src || "/media/project-poster-"+i+".webp"} aria-label={'Prévia do projeto '+p.title}><source data-src={p.video}/></video>}<span className="project-arrow" aria-hidden="true">↗</span></div><div className="project-description"><ul className="project-tags flex flex-wrap">{p.tags.map(t=><li key={t}>{t}</li>)}</ul><h3>{p.title}</h3></div></a></article>)}</div></div></section>}